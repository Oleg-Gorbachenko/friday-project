const initialState: any = {}

export const reducer2 = (state: any = initialState, action: any): any => {
    switch (action.type) {
        case '' : {
            return state
        }
        default: {
            return state
        }
    }
}