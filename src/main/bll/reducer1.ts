const initialState: any = {}

export const reducer1 = (state: any = initialState, action: any): any => {
    switch (action.type) {
        case '' : {
            return state
        }
        default: {
            return state
        }
    }
}